/**
 * This Gradle file is here only so ml-gradle can be used to deploy an application which the JUnit tests can be
 * run against.
 */
plugins {
  id 'net.saliman.properties' version '1.5.2'
  id 'com.marklogic.ml-gradle' version '4.6.0'
}
